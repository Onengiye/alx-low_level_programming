#include <stdio.h>
#include "main.h"

/**
 * add - Adds two integers and return the result
 * @c: first integer
 * @c: second integer
 *
 * Description: Adds two integers and return the result
 * Return: Return the result of the addition
 */
int add(int c, int d)
{
	return (c + d);
}
