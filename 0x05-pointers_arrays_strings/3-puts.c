#include "main.h"
#include <string.h>

/**
 * _puts - prints a string, followed by a new line, to stdout
 * @str: pointer parameters
 *
 * Return: Length of string
*/
void _puts(char *str)
{
	puts(str);
}
