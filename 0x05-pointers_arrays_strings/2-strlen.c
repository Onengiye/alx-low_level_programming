#include "main.h"
#include <string.h>

/**
 * _strlen - function that returns the length of a string
 * @s: pointer parameters
 *
 * Return: Length of string
*/
int _strlen(char *s)
{
	return (strlen(s));
}
